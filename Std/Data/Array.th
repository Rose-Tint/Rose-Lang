module Std.Data.Array where


pure set => [a], Int, a, [a];

pure fill => a, [a], [a];
fill a arr {
    let mut arr'<[a]> := arr;
    let mut i<Int> := 0;
    loop (
        let mut i<Int> := 0,
        i < length arr',
        i = i + 1
    ) set arr' i a;
}
