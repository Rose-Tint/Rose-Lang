

nullptr  {
    return((Ptr 0););
}
pure export nullptr<(Pointer )>
impure export addressOf<(a  Pointer a )>
impure export free<(Pointer a  Void )>
impure export *=<(Pointer a  a  Void )>
impure export deref<(Pointer a  a )>
impure export memSizeOf<(a  Int )>
new a {
    let impure <(DELAYED)>ptr = (malloc (memSizeOf a));
    ((setPtr a) ptr);
    returnptr;
}
impure export new<(a  Pointer a )>
impure export malloc<(Int  Pointer a )>
export data Pointer a    = Ptr internInt 


