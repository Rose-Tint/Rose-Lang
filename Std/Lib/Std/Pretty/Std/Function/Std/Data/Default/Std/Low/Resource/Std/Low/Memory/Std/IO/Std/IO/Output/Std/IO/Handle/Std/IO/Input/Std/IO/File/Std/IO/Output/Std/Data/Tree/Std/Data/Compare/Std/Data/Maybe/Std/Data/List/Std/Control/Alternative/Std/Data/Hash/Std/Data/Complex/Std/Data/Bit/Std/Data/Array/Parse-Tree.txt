

fill a arr {
    loop (let impure <(DELAYED)>i = 0;; (< i (length arr));; i = (+ i 1);) {
        (((set arr) i) a);
    }
    returnarr;
}
pure export fill<(a  [] a  [] a )>
pure export set<(Int  a  [] a  [] a )>

